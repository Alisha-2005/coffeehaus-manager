package com.coffeehaus;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the CoffeeHaus Manager application.
 * Manages specialty coffee roasters and their unique blends.
 */
@SpringBootApplication
public class CoffeeHausApplication {

    public static void main(String[] args) {
        SpringApplication.run(CoffeeHausApplication.class, args);
    }
}
